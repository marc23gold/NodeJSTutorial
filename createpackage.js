//create package dot json file
//JSON holds all metadata for project

//All a package is is reusable code we can include in our application
//it's a folder with one or more modules

const _ = require('lodash');
let example = _.fill([1,2,3,4,5],"banana",1,4);
console.log(example);